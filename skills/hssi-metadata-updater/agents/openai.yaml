interface:
  display_name: "HSSI Metadata Updater"
  short_description: "Refresh, enrich, or target-update HSSI metadata"
  default_prompt: "In PREPARE mode, diff and build an HSSI update payload; in EXECUTE mode, submit an approved update payload and verify the result."
