interface:
  display_name: "HSSI Metadata Submitter"
  short_description: "Build, verify, and submit metadata payloads"
  default_prompt: "In PREPARE mode, build and verify an HSSI submission payload from hssi_metadata.md; in EXECUTE mode, submit an approved payload and verify the roundtrip."
